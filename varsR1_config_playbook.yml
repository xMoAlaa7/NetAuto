---

- hosts: cisco_routers
  gather_facts: false
  tasks:

    - name: Backup configuration
      cisco.ios.ios_config:
        backup: true

    - name: Configure DHCP pools
      import_role:
        name: dhcp

    - name: Debug test
      debug:
        var: confs

